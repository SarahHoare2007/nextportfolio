'use client';
import { createTheme } from '@mui/material/styles';
import { GFS Didot } from '../../../assets/fonts/GFSDidot-Regular.ttf';

const theme = createTheme({
    typography: {

    },
    palette: {
        primary: {
            light: "#9BD7D0",
            main: "#5FA9A0",
            dark: "#3B8377",

        },
    }
})

export default theme;